import { Link } from 'react-router-dom';
import { HeaderContainer } from './style';
import logo from '@/assets/logo.svg';

const Nav = () => {
  return (
    <HeaderContainer>
      <img alt="logo" />
    </HeaderContainer>
  );
};

export default Nav;
